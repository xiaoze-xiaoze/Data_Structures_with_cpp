#include "sortingAlgorithms.hpp"

std::expected<void, DataStructureError> bubbleSort(std::vector<T>& vec, Comparator comp) {
    
}

std::expected<void, DataStructureError> selectionSort(std::vector<T>& vec, Comparator comp) {
    
}

std::expected<void, DataStructureError> insertionSort(std::vector<T>& vec, Comparator comp) {
    
}

std::expected<void, DataStructureError> shellSort(std::vector<T>& vec, Comparator comp) {
    
}

std::expected<void, DataStructureError> mergeSort(std::vector<T>& vec, Comparator comp) {
    
}

std::expected<void, DataStructureError> quickSort(std::vector<T>& vec, Comparator comp) {
    
}

std::expected<void, DataStructureError> heapSort(std::vector<T>& vec, Comparator comp) {
    
}